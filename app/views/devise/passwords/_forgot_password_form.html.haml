= form_for(resource, as: resource_name, url: password_path(resource_name), name: 'forgot-password-form', class: 'main-contact-form.contact-form') do |f|
  .form-group
    = f.label :email
    = f.email_field :email, autofocus: true, autocomplete: 'email', class: 'form-control'
    %hr

  .form-group
    %button.btn.btn-primary.btn-lg{:name => "submit", :required => "required", :type => "submit"}
      = I18n.t('forms.forgot_password.send_instructions')