.block-item
  .block-content#article_name
    %h2
      = Article.human_attribute_name(:name)
    .block-visible-data
      = simple_format(@resource.name)
    .block-hidden-form{ 'data-params': { block_id: 'article_name' } }
      = form_with(model: [@user, @resource]) do |f|
        = f.hidden_field(:user_id)
        .form-group.required
          = f.text_field(:name, class: 'form-control')
          .field_hint
            Под этим именем вашу программу увидят заказчики
        %div
          = link_to('#', class: 'btn btn-cancel') do
            = I18n.t('cancel')
          %button.btn.btn-primary{:name => "submit", :required => "required", :type => "submit" }
            = I18n.t('forms.article_new.create')
